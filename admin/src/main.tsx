import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// TODO: implementar (montar o painel admin)
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
